# Workflow Index

Primary workflows:

- `continue-next-story.md`
- `story-lookup.md`
- `story-sizing.md`
- `tdd-pipeline.md`

The other workflow files remain only as compatibility stubs for older references.

Specs are optional in the current workflow. Use them when the work needs them, not by default.
