# Claude Agent Mirror

Use `AGENTS.md` and `CLAUDE.md` as the real contract.

Claude's short version:

- full agent, not UI-only
- checked-in docs over memory
- work directly on a fresh story branch by default
- use sibling worktrees only for parallel tasks or dirty-checkout isolation
- use historical handoff docs only as optional context
