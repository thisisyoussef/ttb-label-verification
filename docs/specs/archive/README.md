# Archived Spec Packets

This folder holds story packets that were explicitly removed from the active queue.

- `TTB-212/` was archived on 2026-04-15 after the user scrapped the local-model direction.

Archived packets are historical context only. They are not active dependencies, not next-story candidates, and should not be resumed unless the user explicitly asks for that work again.
