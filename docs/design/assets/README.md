# Design Assets

Checked-in reference artifacts for the theme and design spec.

## Expected files

- `industrial-precision-theme.png` — Stitch theme-picker screenshot captured during the TTB-101 Stitch run on 2026-04-13. Missing as of this commit; the temp path from the session (`/var/folders/.../TemporaryItems/...`) was cleaned up by macOS before it could be copied. Drop the PNG here if you re-capture it.

## Rules

- Keep assets small. If an image is larger than ~500 KB, compress it first.
- Do not delete historical assets when the theme changes — rename the old one with a date suffix.
- Do not store uploaded label photos, reviewer inputs, or verification results here. This folder is for design-time reference only.
