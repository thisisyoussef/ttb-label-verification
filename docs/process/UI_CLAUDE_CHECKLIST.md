# Legacy UI Checklist Stub

This file exists only so older references fail closed instead of reviving the previous lane-split workflow.

Current rule:

- Do not treat Claude as a UI-only lane.
- Do not block on a Codex handoff.
- Use `AGENTS.md`, `docs/process/SINGLE_SOURCE_OF_TRUTH.md`, `docs/process/CODEX_CHECKLIST.md`, and `docs/process/STITCH_AUTOMATION.md` as the active contract.
- Stop only for real prerequisites such as explicit user visual approval or missing returned Stitch assets.
